# --- Core ---
Django==6.0.6

# --- Production server & static files ---
gunicorn==26.0.0
whitenoise==6.7.0

# --- Optional production database (uncomment for Postgres) ---
# psycopg[binary]==3.2.3

# --- Config ---
# 12-factor style env parsing; falls back gracefully if absent.
django-environ==0.11.2
